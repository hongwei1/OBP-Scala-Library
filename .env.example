# Nexus Repository Credentials
# Copy this file to .env and update with your actual credentials
# DO NOT commit .env to version control!

# Nexus Authentication
NEXUS_USERNAME=admin
NEXUS_PASSWORD=your-nexus-password-here

# Nexus Connection Settings
# For Docker environment (when running SBT in Docker container)
NEXUS_HOST=nexus
NEXUS_URL=http://nexus:8081/

# For local development (when running SBT directly on host)
# NEXUS_HOST=localhost
# NEXUS_URL=http://localhost:8081/

# For production (example)
# NEXUS_HOST=nexus.yourcompany.com
# NEXUS_URL=https://nexus.yourcompany.com/

# Development credentials (current setup)
# Username: admin
# Password: 20b05303-d54d-434e-8aa6-48cc9ed3de20

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your credentials
# 3. Source the file: source .env
# 4. Run SBT: sbt publish
#
# Or for Docker:
# docker-compose run --rm -T --env-file .env sbt sbt publish
